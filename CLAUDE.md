# Claude Code Instructions

See [README.md](README.md) for full project documentation.
Make sure new features are implemented efficiently, both in terms of token usage as well as in terms of code structure and maintainability.
Use DRY principles.

## Testing

Run `pnpm test` to execute unit tests.

**Important**: Before finishing a coding session or when the user asks to commit changes, run `pnpm test` to ensure all tests pass.
